require("config.vim-options")
require("config.lazy")

-- Load snippets --------------------------------------------------------------
-- for _, path in ipairs(vim.api.nvim_get_runtime_file("snippets/*.lua", true)) do
--     loadfile(path)()
-- end
